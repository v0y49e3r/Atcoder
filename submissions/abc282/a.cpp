#include <bits/stdc++.h>
#include <cctype>
#include <climits>
#include <cmath>
using namespace std;

int main() {
  int k;
  cin >> k;
  for (int i = 0; i < k; ++i) {
    cout << char('A' + i);
  }
  return 0;
}
